# 🔧 แก้ไขปัญหา Connection

## ปัญหาที่พบ

1. **PostgreSQL 18** ไม่ได้รันอยู่ (Connection refused on port 5433)
2. **PostgreSQL 17** เชื่อมต่อได้แล้ว ✅ (เห็น database "cmu sharecycle")
3. ยังไม่มีตาราง (ต้องรัน migration)

---

## ✅ วิธีแก้ไข: ใช้ PostgreSQL 17 แทน

### ขั้นตอนที่ 1: ตรวจสอบว่า PostgreSQL 17 กำลังรันอยู่

```bash
# ตรวจสอบ services
brew services list

# ดูว่า PostgreSQL 17 รันอยู่หรือไม่
# ถ้าไม่รัน ให้เริ่ม:
brew services start postgresql@17
```

### ขั้นตอนที่ 2: ดูข้อมูลการเชื่อมต่อ PostgreSQL 17

1. ใน pgAdmin4:
   - คลิกขวาที่ **"PostgreSQL 17"** → **"Properties"**
   - ไปที่แท็บ **"Connection"**
   - ดูข้อมูล:
     - **Host**: `localhost`
     - **Port**: `5432` (ไม่ใช่ 5433!)
     - **Username**: `pmykingg` (หรือ `postgres`)
     - **Password**: (รหัสผ่านของคุณ)

### ขั้นตอนที่ 3: สร้างไฟล์ .env

สร้างไฟล์ `.env` ในโฟลเดอร์ `backend/`:

```env
PORT=4000
CLIENT_ORIGIN=http://localhost:3000
DATABASE_URL=postgresql://pmykingg:รหัสผ่านของคุณ@localhost:5432/cmu%20sharecycle
JWT_SECRET=cmu-sharecycle-secret-key-2025-min-16-chars
```

**⚠️ สำคัญ:**
- ใช้ port **5432** (ไม่ใช่ 5433)
- ใช้ username `pmykingg` (หรือ username ที่คุณใช้ใน PostgreSQL 17)
- ชื่อ database มีช่องว่าง ใช้ `%20` แทน: `cmu%20sharecycle`

### ขั้นตอนที่ 4: รัน Migration เพื่อสร้างตาราง

```bash
cd /Users/pmykingg/Documents/project/hackkathon2025byg4/backend
npm run db:migrate
```

ถ้าสำเร็จจะเห็น:
```
Database migrated successfully
```

### ขั้นตอนที่ 5: ตรวจสอบตารางใน pgAdmin4

1. ใน pgAdmin4:
   - ขยาย **"PostgreSQL 17"** → **"Databases"** → **"cmu sharecycle"**
   - ขยาย **"Schemas"** → **"public"** → **"Tables"**
   - คลิกขวาที่ **"Tables"** → **"Refresh"**
   - คุณควรเห็นตาราง:
     - `users`
     - `items`
     - `exchange_requests`
     - `exchange_history`
     - `chats`
     - `messages`
     - `notifications`

---

## 🚀 วิธีง่ายๆ: ใช้สคริปต์ช่วยตั้งค่า

รันคำสั่งนี้แล้วสคริปต์จะช่วยตั้งค่าทั้งหมดให้:

```bash
cd /Users/pmykingg/Documents/project/hackkathon2025byg4/backend
npm run db:setup
```

กรอกข้อมูล:
- **Host**: `localhost`
- **Port**: `5432` (ไม่ใช่ 5433!)
- **Username**: `pmykingg`
- **Password**: (รหัสผ่านของคุณ)
- **Database**: `cmu sharecycle`

สคริปต์จะ:
- ทดสอบการเชื่อมต่อ
- รัน migration สร้างตารางให้อัตโนมัติ
- สร้างไฟล์ `.env` ให้อัตโนมัติ

---

## 🔍 ตรวจสอบว่า PostgreSQL กำลังรันอยู่

```bash
# ตรวจสอบ services
brew services list

# ตรวจสอบ PostgreSQL 17
pg_isready -p 5432

# ถ้าไม่รัน ให้เริ่ม:
brew services start postgresql@17

# หรือถ้าเป็น PostgreSQL 18:
brew services start postgresql@18
```

---

## ⚠️ หมายเหตุ

- **PostgreSQL 18** ใช้ port **5433** (ไม่ใช่ 5432)
- **PostgreSQL 17** ใช้ port **5432** (default)
- ถ้าต้องการใช้ PostgreSQL 18 ต้องเริ่ม service ก่อน:
  ```bash
  brew services start postgresql@18
  ```
- แต่แนะนำให้ใช้ **PostgreSQL 17** เพราะดูเหมือนจะตั้งค่าไว้แล้ว

---

## ✅ Checklist

- [ ] ตรวจสอบว่า PostgreSQL 17 กำลังรันอยู่
- [ ] ดูข้อมูล connection จาก pgAdmin4 (PostgreSQL 17)
- [ ] สร้างไฟล์ `.env` ด้วยข้อมูลที่ถูกต้อง (port 5432)
- [ ] รัน `npm run db:migrate` สำเร็จ
- [ ] เห็นตารางใน pgAdmin4 แล้ว
- [ ] รัน `npm run db:test` เพื่อทดสอบการเชื่อมต่อ

