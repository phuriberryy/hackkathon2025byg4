# 🔄 วิธีการทำงาน: ข้อมูลจาก Frontend → Database

## ✅ คำตอบสั้นๆ: **ใช่ครับ!** ข้อมูลจะถูกเก็บใน Database

เมื่อคุณกรอกข้อมูลใน Frontend และกด Submit ข้อมูลจะถูกส่งไปยัง Backend API และบันทึกลง Database อัตโนมัติ

---

## 📊 Flow การทำงาน (Flow Diagram)

```
┌─────────────┐         ┌─────────────┐         ┌─────────────┐         ┌─────────────┐
│   Frontend  │  ────>  │   Backend   │  ────>  │  Database   │  ────>  │   Result    │
│  (React)    │  POST   │   (Express) │  INSERT │ (PostgreSQL)│  RETURN │   (JSON)    │
└─────────────┘         └─────────────┘         └─────────────┘         └─────────────┘
     User                    API Server              Tables                    Response
   กรอกฟอร์ม              ตรวจสอบข้อมูล            บันทึกข้อมูล            ส่งผลลัพธ์กลับ
```

---

## 🎯 ตัวอย่างการทำงาน

### 1. **การลงทะเบียน (Register)**

**Frontend** (`RegisterPage.jsx`):
```javascript
// ผู้ใช้กรอกข้อมูล
const payload = {
  name: "สมชาย",
  email: "somchai@cmu.ac.th",
  password: "password123",
  faculty: "วิศวกรรมศาสตร์"
}

// ส่งไปยัง Backend
await authApi.register(payload)
```

**Backend** (`authController.js`):
```javascript
// รับข้อมูลจาก Frontend
const { name, email, password, faculty } = req.body

// บันทึกลง Database
await query(
  `INSERT INTO users (name, faculty, email, password_hash)
   VALUES ($1, $2, $3, $4)`,
  [name, faculty, email, passwordHash]
)
```

**Database** (`users` table):
```
| id  | name   | email              | faculty      | created_at          |
|-----|--------|--------------------|--------------|---------------------|
| uuid| สมชาย  | somchai@cmu.ac.th  | วิศวกรรมศาสตร์| 2025-11-13 14:00:00 |
```

---

### 2. **การโพสต์สินค้า (Post Item)**

**Frontend** (`PostItemModal.jsx`):
```javascript
// ผู้ใช้กรอกข้อมูลสินค้า
const payload = {
  title: "หนังสือเรียน",
  category: "หนังสือ",
  itemCondition: "ดี",
  description: "หนังสือเรียนเก่า",
  pickupLocation: "คณะวิศวกรรมศาสตร์"
}

// ส่งไปยัง Backend (พร้อม token สำหรับ authentication)
await itemsApi.create(token, payload)
```

**Backend** (`itemController.js`):
```javascript
// ตรวจสอบว่า user login แล้ว
if (!req.user) {
  return res.status(401).json({ message: 'Unauthorized' })
}

// บันทึกลง Database
await query(
  `INSERT INTO items (user_id, title, category, item_condition, description, pickup_location)
   VALUES ($1, $2, $3, $4, $5, $6)`,
  [req.user.id, title, category, itemCondition, description, pickupLocation]
)
```

**Database** (`items` table):
```
| id  | user_id | title    | category | item_condition | created_at          |
|-----|---------|----------|----------|----------------|---------------------|
| uuid| user_id | หนังสือเรียน | หนังสือ | ดี            | 2025-11-13 14:05:00 |
```

---

### 3. **การส่งคำขอแลกเปลี่ยน (Exchange Request)**

**Frontend** (`ExchangeRequestModal.jsx`):
```javascript
const payload = {
  itemId: "uuid-of-item",
  message: "ต้องการแลกเปลี่ยนครับ"
}

await exchangeApi.request(token, payload)
```

**Backend** (`exchangeController.js`):
```javascript
await query(
  `INSERT INTO exchange_requests (item_id, requester_id, message)
   VALUES ($1, $2, $3)`,
  [itemId, req.user.id, message]
)
```

**Database** (`exchange_requests` table):
```
| id  | item_id | requester_id | message              | status  | created_at          |
|-----|---------|--------------|----------------------|---------|---------------------|
| uuid| item_id | user_id      | ต้องการแลกเปลี่ยนครับ | pending | 2025-11-13 14:10:00 |
```

---

## 🔍 ตรวจสอบข้อมูลใน Database

### วิธีที่ 1: ใช้ pgAdmin4

1. เปิด pgAdmin4
2. ขยาย **PostgreSQL 17** → **Databases** → **cmu sharecycle**
3. ขยาย **Schemas** → **public** → **Tables**
4. คลิกขวาที่ตาราง (เช่น `users`) → **View/Edit Data** → **All Rows**
5. คุณจะเห็นข้อมูลทั้งหมดที่ถูกบันทึกไว้

### วิธีที่ 2: ใช้ Terminal (psql)

```bash
# เชื่อมต่อกับ database
psql -U pmykingg -d "cmu sharecycle"

# ดูข้อมูลในตาราง users
SELECT * FROM users;

# ดูข้อมูลในตาราง items
SELECT * FROM items;

# ดูข้อมูลในตาราง exchange_requests
SELECT * FROM exchange_requests;

# ออก
\q
```

### วิธีที่ 3: ใช้ API Endpoint

```bash
# ดู items ทั้งหมด
curl http://localhost:4000/api/items

# ดู item โดย ID
curl http://localhost:4000/api/items/{itemId}
```

---

## 📋 API Endpoints ที่บันทึกข้อมูลลง Database

### Authentication
- `POST /api/auth/register` → บันทึกในตาราง `users`
- `POST /api/auth/login` → อ่านจากตาราง `users` (ไม่บันทึก)

### Items
- `POST /api/items` → บันทึกในตาราง `items`
- `PUT /api/items/:itemId` → อัพเดทในตาราง `items`
- `DELETE /api/items/:itemId` → ลบจากตาราง `items`

### Exchange
- `POST /api/exchange` → บันทึกในตาราง `exchange_requests`
- `POST /api/exchange/:requestId/accept-owner` → อัพเดทในตาราง `exchange_requests`
- `POST /api/exchange/:requestId/accept-requester` → อัพเดทในตาราง `exchange_requests` และ `exchange_history`

### Chats
- `POST /api/chats` → บันทึกในตาราง `chats`
- `POST /api/chats/:chatId/messages` → บันทึกในตาราง `messages`

### Notifications
- ระบบจะสร้าง notification อัตโนมัติเมื่อมี:
  - คำขอแลกเปลี่ยนใหม่ → บันทึกในตาราง `notifications`
  - ข้อความใหม่ → บันทึกในตาราง `notifications`

---

## ✅ Checklist: ตรวจสอบว่าทุกอย่างทำงาน

1. **Backend Server รันอยู่**
   ```bash
   cd backend
   npm run dev
   ```
   ควรเห็น: `Backend listening on port 4000`

2. **Database เชื่อมต่อสำเร็จ**
   ควรเห็น: `✅ Database connected successfully!`

3. **Frontend รันอยู่**
   ```bash
   cd frontend
   npm run dev
   ```

4. **ทดสอบการบันทึกข้อมูล**
   - ลงทะเบียนผู้ใช้ใหม่
   - โพสต์สินค้า
   - ตรวจสอบใน pgAdmin4 ว่ามีข้อมูลหรือไม่

---

## 🐛 แก้ไขปัญหา

### ปัญหา: ข้อมูลไม่ถูกบันทึก

**ตรวจสอบ:**
1. Backend server รันอยู่หรือไม่
2. Database เชื่อมต่อสำเร็จหรือไม่ (ดู log ใน terminal)
3. มี error ใน browser console หรือไม่
4. ตรวจสอบ Network tab ใน browser ว่า API call สำเร็จหรือไม่

### ปัญหา: Error 401 (Unauthorized)

**แก้ไข:**
- ต้อง login ก่อนถึงจะบันทึกข้อมูลได้
- ตรวจสอบว่า token ถูกส่งไปใน header หรือไม่

### ปัญหา: Error 400 (Bad Request)

**แก้ไข:**
- ตรวจสอบว่าข้อมูลที่ส่งไปครบถ้วนหรือไม่
- ตรวจสอบ validation rules ใน routes

---

## 📝 สรุป

**ใช่ครับ!** เมื่อคุณกรอกข้อมูลใน Frontend และกด Submit:

1. ✅ ข้อมูลถูกส่งไปยัง Backend API
2. ✅ Backend ตรวจสอบข้อมูล (validation)
3. ✅ Backend บันทึกลง Database (INSERT/UPDATE)
4. ✅ Database เก็บข้อมูลในตาราง
5. ✅ Backend ส่งผลลัพธ์กลับไปยัง Frontend
6. ✅ Frontend แสดงผลลัพธ์ให้ผู้ใช้เห็น

**คุณสามารถตรวจสอบข้อมูลได้ใน pgAdmin4 หรือใช้ psql ใน Terminal!** 🎉

