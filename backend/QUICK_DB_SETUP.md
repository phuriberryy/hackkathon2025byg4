# 🚀 คู่มือตั้งค่าฐานข้อมูลแบบง่าย (สำหรับผู้ใช้ pgAdmin4)

## ✅ สถานะปัจจุบัน
- คุณมี database ชื่อ **"cmu sharecycle"** อยู่แล้ว ✅
- แต่ยังไม่มีตาราง (ต้องรัน migration) ⚠️

---

## ขั้นตอนที่ 1: หาข้อมูลการเชื่อมต่อจาก pgAdmin4

### วิธีดู Connection Details:

1. **ใน pgAdmin4** คลิกขวาที่ **"PostgreSQL 17"** (หรือ server ที่คุณใช้)
2. เลือก **"Properties"** หรือ **"Properties..."**
3. ไปที่แท็บ **"Connection"**
4. คุณจะเห็นข้อมูล:
   - **Host name/address**: `localhost` (หรือ IP address)
   - **Port**: `5432` (หรือ port อื่น)
   - **Maintenance database**: `postgres`
   - **Username**: `postgres` (หรือ username อื่น)
   - **Password**: ⚠️ ถ้าเป็นช่องว่าง = ไม่มีรหัสผ่าน

### ถ้าไม่รู้รหัสผ่านหรือต้องการตั้งรหัสผ่านใหม่:

**วิธีที่ 1: ตั้งรหัสผ่านใหม่ใน pgAdmin4 (แนะนำ)**

1. ใน pgAdmin4 คลิกขวาที่ **"PostgreSQL 17"** → **"Properties"**
2. ไปที่แท็บ **"Connection"**
3. ใส่รหัสผ่านใหม่ในช่อง **"Password"** (เช่น `password123` หรือรหัสที่คุณจำได้)
4. ✅ ติ๊ก **"Save password?"** เพื่อบันทึกรหัสผ่าน
5. คลิก **"Save"**

**วิธีที่ 2: ใช้ Terminal ตั้งรหัสผ่าน**

```bash
# เชื่อมต่อกับ PostgreSQL (ใช้ username ของคุณ)
psql -U pmykingg -d postgres

# ถ้าไม่มีรหัสผ่านจะไม่ต้องใส่ password
# ถ้ามีรหัสผ่านแต่ลืม ให้ลองกด Enter หรือใช้วิธีอื่น

# ตั้งรหัสผ่านใหม่ (ใช้ username ของคุณ)
ALTER USER pmykingg WITH PASSWORD 'รหัสผ่านที่คุณต้องการ';

# ออก
\q
```

**วิธีที่ 3: ใช้สคริปต์ช่วยตั้งค่า (ง่ายที่สุด!)**

รันคำสั่งนี้แล้วสคริปต์จะถามข้อมูลทั้งหมดให้:
```bash
cd /Users/pmykingg/Documents/project/hackkathon2025byg4/backend
npm run db:setup
```

สคริปต์จะถาม:
- Username: ใส่ `pmykingg`
- Password: ถ้าไม่มีให้กด Enter หรือใส่รหัสผ่านใหม่
- Database: ใส่ `cmu sharecycle` หรือ `sharecycle`

---

## ขั้นตอนที่ 2: สร้างไฟล์ .env

สร้างไฟล์ `.env` ในโฟลเดอร์ `backend/`:

```bash
cd /Users/pmykingg/Documents/project/hackkathon2025byg4/backend
```

### ถ้ามีรหัสผ่าน:
```env
PORT=4000
CLIENT_ORIGIN=http://localhost:3000
DATABASE_URL=postgresql://pmykingg:รหัสผ่านของคุณ@localhost:5432/cmu%20sharecycle
JWT_SECRET=cmu-sharecycle-secret-key-2025-min-16-chars
```

**⚠️ สำคัญ:** 
- Username ของคุณคือ `pmykingg` (ไม่ใช่ `postgres`)
- ชื่อ database มีช่องว่าง ต้องใช้ `%20` แทนช่องว่าง หรือเปลี่ยนชื่อ database

### ถ้าไม่มีรหัสผ่าน (ไม่แนะนำ แต่ใช้ได้):
```env
PORT=4000
CLIENT_ORIGIN=http://localhost:3000
DATABASE_URL=postgresql://pmykingg@localhost:5432/cmu%20sharecycle
JWT_SECRET=cmu-sharecycle-secret-key-2025-min-16-chars
```

**⚠️ หมายเหตุ:** ชื่อ database ของคุณคือ **"cmu sharecycle"** (มีช่องว่าง) ต้องใส่ใน quotes หรือใช้ `%20` แทนช่องว่าง:

```env
# วิธีที่ 1: ใช้ %20 แทนช่องว่าง (ใช้ username pmykingg)
DATABASE_URL=postgresql://pmykingg:password@localhost:5432/cmu%20sharecycle

# วิธีที่ 2: ใช้ชื่อ database ใหม่ที่ไม่มีช่องว่าง (แนะนำมากกว่า)
# เปลี่ยนชื่อ database ใน pgAdmin4 เป็น "cmusharecycle" หรือ "sharecycle"
DATABASE_URL=postgresql://pmykingg:password@localhost:5432/sharecycle
```

---

## ขั้นตอนที่ 3: เปลี่ยนชื่อ Database (แนะนำ)

ชื่อ database ที่มีช่องว่างอาจทำให้เกิดปัญหา ควรเปลี่ยนเป็นชื่อที่ไม่มีช่องว่าง:

### ใน pgAdmin4:
1. คลิกขวาที่ **"cmu sharecycle"** → **"Properties"**
2. เปลี่ยนชื่อเป็น **"sharecycle"** หรือ **"cmusharecycle"**
3. คลิก **"Save"**

### หรือสร้างใหม่:
1. คลิกขวาที่ **"Databases"** → **"Create"** → **"Database..."**
2. ตั้งชื่อ: **"sharecycle"**
3. Owner: **"postgres"**
4. คลิก **"Save"**

แล้วใช้ใน `.env`:
```env
DATABASE_URL=postgresql://postgres:password@localhost:5432/sharecycle
```

---

## ขั้นตอนที่ 4: รัน Migration เพื่อสร้างตาราง

```bash
cd /Users/pmykingg/Documents/project/hackkathon2025byg4/backend
npm run db:migrate
```

ถ้าสำเร็จจะเห็น:
```
Database migrated successfully
```

---

## ขั้นตอนที่ 5: ตรวจสอบตาราง

### ใน pgAdmin4:
1. ขยาย **"PostgreSQL 17"** → **"Databases"** → **"sharecycle"** (หรือชื่อ database ของคุณ)
2. ขยาย **"Schemas"** → **"public"** → **"Tables"**
3. คุณควรเห็นตาราง:
   - `users`
   - `items`
   - `exchange_requests`
   - `exchange_history`
   - `chats`
   - `messages`
   - `notifications`

### ทดสอบการเชื่อมต่อ:
```bash
npm run db:test
```

---

## 🔧 แก้ไขปัญหา

### ปัญหา: "database does not exist"
- ตรวจสอบชื่อ database ใน `.env` ว่าตรงกับใน pgAdmin4 หรือไม่
- ถ้าชื่อมีช่องว่าง ใช้ `%20` แทน หรือเปลี่ยนชื่อ database

### ปัญหา: "password authentication failed"
- ตรวจสอบรหัสผ่านใน `.env`
- ลองตั้งรหัสผ่านใหม่ใน pgAdmin4

### ปัญหา: "connection refused"
- ตรวจสอบว่า PostgreSQL กำลังรัน:
  ```bash
  brew services list
  ```
- ถ้าไม่ได้รัน:
  ```bash
  brew services start postgresql@17
  # หรือ
  brew services start postgresql@18
  ```

### ปัญหา: ตารางไม่ขึ้นหลังรัน migration
- ตรวจสอบว่าเลือก database ที่ถูกต้องใน pgAdmin4
- ลอง refresh (คลิกขวาที่ "Tables" → "Refresh")
- ตรวจสอบ error message ใน terminal ตอนรัน migration

---

## 📝 Checklist

- [ ] ดู connection details จาก pgAdmin4 แล้ว
- [ ] ตั้งรหัสผ่าน PostgreSQL แล้ว (ถ้ายังไม่มี)
- [ ] เปลี่ยนชื่อ database เป็นชื่อที่ไม่มีช่องว่าง (แนะนำ)
- [ ] สร้างไฟล์ `.env` แล้ว
- [ ] ตั้งค่า `DATABASE_URL` ใน `.env` แล้ว
- [ ] รัน `npm run db:migrate` สำเร็จแล้ว
- [ ] เห็นตารางใน pgAdmin4 แล้ว
- [ ] รัน `npm run db:test` สำเร็จแล้ว

---

## 🎯 ตัวอย่างไฟล์ .env ที่สมบูรณ์

```env
# Server
PORT=4000
CLIENT_ORIGIN=http://localhost:3000

# Database (ตัวอย่าง - แก้ไขตามของคุณ)
DATABASE_URL=postgresql://postgres:mypassword123@localhost:5432/sharecycle

# JWT Secret
JWT_SECRET=cmu-sharecycle-super-secret-key-2025-hackathon-g4

# Email (Optional - ใส่ทีหลังได้)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=your-email@gmail.com
EMAIL_PASS=your-app-password
EMAIL_FROM=CMU ShareCycle <your-email@gmail.com>
```

