# 🔧 แก้ไขปัญหา: Port 4000 ถูกใช้งานอยู่แล้ว

## ❌ Error ที่เจอ

```
Error: listen EADDRINUSE: address already in use :::4000
```

**สาเหตุ:** มี backend server รันอยู่แล้วที่ port 4000

---

## ✅ วิธีแก้ไข

### วิธีที่ 1: ใช้สคริปต์ช่วย (แนะนำ)

```bash
cd /Users/pmykingg/Documents/project/hackkathon2025byg4/backend
npm run kill:port
```

สคริปต์จะ:
- หา process ที่ใช้ port 4000
- แสดงข้อมูล process
- Kill process นั้น
- ตรวจสอบว่า port ว่างแล้ว

### วิธีที่ 2: ใช้คำสั่ง Terminal โดยตรง

```bash
# หา process ID ที่ใช้ port 4000
lsof -ti:4000

# Kill process (แทนที่ PID ด้วยตัวเลขที่ได้)
kill PID

# หรือ force kill ถ้ายังไม่หยุด
kill -9 PID
```

### วิธีที่ 3: Kill ทั้งหมดที่ใช้ port 4000

```bash
# Kill ทุก process ที่ใช้ port 4000
lsof -ti:4000 | xargs kill -9
```

---

## 🔍 ตรวจสอบว่า Port ว่างแล้ว

```bash
# ตรวจสอบว่า port 4000 ว่างหรือไม่
lsof -ti:4000

# ถ้าไม่มี output = port ว่างแล้ว ✅
# ถ้ามี output = ยังมี process ใช้อยู่ ⚠️
```

---

## 🚀 หลังจากแก้ไขแล้ว

รัน backend server ใหม่:

```bash
cd /Users/pmykingg/Documents/project/hackkathon2025byg4/backend
npm run dev
```

หรือ

```bash
cd /Users/pmykingg/Documents/project/hackkathon2025byg4
npm run start:backend
```

---

## 💡 วิธีป้องกันในอนาคต

### 1. ตรวจสอบก่อนรัน

```bash
# ตรวจสอบว่า port 4000 ว่างหรือไม่
lsof -ti:4000 && echo "Port ถูกใช้งานอยู่" || echo "Port ว่าง"
```

### 2. ใช้สคริปต์ kill-port ก่อนรัน

```bash
npm run kill:port && npm run dev
```

### 3. เปลี่ยน Port (ถ้าจำเป็น)

แก้ไขในไฟล์ `.env`:
```env
PORT=4001  # เปลี่ยนเป็น port อื่น
```

---

## 📝 Checklist

- [ ] หา process ที่ใช้ port 4000
- [ ] Kill process นั้น
- [ ] ตรวจสอบว่า port ว่างแล้ว
- [ ] รัน backend server ใหม่
- [ ] ตรวจสอบว่า server รันสำเร็จ

---

## 🎯 สรุป

**ปัญหา:** Port 4000 ถูกใช้งานอยู่แล้ว  
**แก้ไข:** Kill process ที่ใช้ port 4000  
**คำสั่ง:** `npm run kill:port` หรือ `lsof -ti:4000 | xargs kill -9`

