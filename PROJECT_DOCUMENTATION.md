# CMU ShareCycle - เอกสารสรุปโปรเจค

## 📋 สารบัญ
1. [เทคโนโลยีที่ใช้](#เทคโนโลยีที่ใช้)
2. [สถาปัตยกรรมระบบ](#สถาปัตยกรรมระบบ)
3. [ฟีเจอร์ทั้งหมด](#ฟีเจอร์ทั้งหมด)
4. [การทำงานของแต่ละฟีเจอร์](#การทำงานของแต่ละฟีเจอร์)
5. [ฐานข้อมูล](#ฐานข้อมูล)

---

## 🛠️ เทคโนโลยีที่ใช้

### Backend
- **Runtime**: Node.js (ES Modules)
- **Framework**: Express.js 4.19.2
- **Database**: PostgreSQL (pg 8.12.0)
- **Authentication**: JWT (jsonwebtoken 9.0.2)
- **Password Hashing**: bcryptjs 2.4.3
- **Real-time Communication**: Socket.io 4.7.5
- **Email Service**: Nodemailer 6.9.15
- **Validation**: express-validator 7.2.1, Zod 3.23.8
- **CORS**: cors 2.8.5
- **Environment**: dotenv 16.4.7
- **UUID**: uuid 11.0.3

### Frontend
- **Framework**: React 18.3.1
- **Routing**: React Router DOM 7.9.5
- **Styling**: Tailwind CSS 3.4.14
- **Icons**: Lucide React 0.553.0
- **Real-time**: Socket.io Client 4.7.5
- **QR Code**: 
  - qrcode.react 4.2.0 (สร้าง QR Code)
  - html5-qrcode 2.3.8 (สแกน QR Code)
- **Build Tool**: Create React App (CRA) with CRACO 7.1.0

### Database
- **Database**: PostgreSQL
- **Extensions**: uuid-ossp (สำหรับ UUID generation)

---

## 🏗️ สถาปัตยกรรมระบบ

### Backend Structure
```
backend/
├── src/
│   ├── controllers/     # Business logic
│   ├── routes/          # API routes
│   ├── middleware/      # Authentication, validation
│   ├── services/        # Chat service (Socket.io)
│   ├── utils/           # Utilities (email, CO2, moderation)
│   ├── db/              # Database connection
│   ├── config/          # Configuration
│   └── app.js           # Express app setup
└── sql/                 # Database migrations
```

### Frontend Structure
```
frontend/
├── src/
│   ├── pages/           # Page components
│   ├── components/       # Reusable components
│   │   ├── modals/       # Modal components
│   │   ├── layout/       # Layout components
│   │   └── ui/           # UI components
│   ├── context/          # React Context (Auth)
│   ├── lib/              # API utilities
│   └── utils/            # Frontend utilities
```

---

## ✨ ฟีเจอร์ทั้งหมด

### 1. Authentication & User Management
- ลงทะเบียน (Register)
- เข้าสู่ระบบ (Login)
- ออกจากระบบ (Logout)
- จัดการโปรไฟล์ (Edit Profile)

### 2. Item Management
- โพสต์สินค้า (Post Item)
- แก้ไขโพสต์ (Edit Item)
- ลบโพสต์ (Delete Item)
- ดูรายละเอียดสินค้า (View Item Details)
- กรองสินค้า (Filter Items by Category)
- ค้นหาสินค้า (Search Items)
- จัดการโพสต์ (Manage Posts)
- ระบบหมดอายุอัตโนมัติ (Auto Expiration)

### 3. Exchange System
- ขอแลกเปลี่ยน (Request Exchange)
- ยอมรับ/ปฏิเสธคำขอแลกเปลี่ยน (Accept/Reject Exchange Request)
- ประวัติการแลกเปลี่ยน (Exchange History)
- QR Code สำหรับยืนยันการแลกเปลี่ยน

### 4. Donation System
- โพสต์สินค้าบริจาค (Post Donation Item)
- ขอรับบริจาค (Request Donation)
- ยอมรับ/ปฏิเสธคำขอรับบริจาค (Accept/Reject Donation Request)
- ประวัติการบริจาค (Donation History)
- QR Code สำหรับยืนยันการบริจาค

### 5. Real-time Chat
- แชทแบบ Real-time (Socket.io)
- ส่งข้อความ (Send Messages)
- ดูประวัติแชท (Chat History)
- ยอมรับ/ปฏิเสธแชท (Accept/Decline Chat)

### 6. QR Code System
- สร้าง QR Code (Generate QR Code)
- สแกน QR Code (Scan QR Code)
- ยืนยัน QR Code (Confirm QR Code)
- รองรับทั้ง Exchange และ Donation

### 7. Notification System
- แจ้งเตือนแบบ Real-time (Real-time Notifications)
- ประเภทการแจ้งเตือน:
  - Exchange Request
  - Exchange Accepted/Rejected
  - Donation Request
  - Donation Accepted/Rejected
  - Chat Messages
- ทำเครื่องหมายว่าอ่านแล้ว (Mark as Read)

### 8. CO₂ Tracking
- คำนวณ CO₂ Footprint ของสินค้า
- คำนวณ CO₂ ที่ลดได้จากการแลกเปลี่ยน/บริจาค
- แสดงสถิติ CO₂ (Statistics)

### 9. Content Moderation
- ตรวจสอบคำต้องห้าม (Spam Detection)
- ตรวจสอบเนื้อหาซ้ำ (Duplicate Content Check)
- ตรวจสอบรูปภาพ (Image Validation)
- ป้องกันการโพสต์สแปม

### 10. Email Service
- ส่งอีเมลแจ้งเตือน (Email Notifications)
- รีเซ็ตรหัสผ่าน (Password Reset)
- รองรับ Gmail และ SMTP

### 11. Statistics
- สถิติการแลกเปลี่ยน/บริจาค
- สถิติ CO₂ ที่ลดได้
- สถิติผู้ใช้และสินค้า

---

## 🔧 การทำงานของแต่ละฟีเจอร์

### 1. Authentication & User Management

#### 1.1 ลงทะเบียน (Register)
**เทคโนโลยี**: JWT, bcryptjs, express-validator
- ผู้ใช้กรอกข้อมูล: ชื่อ, อีเมล, รหัสผ่าน, คณะ
- ระบบตรวจสอบความถูกต้องของข้อมูล
- Hash รหัสผ่านด้วย bcryptjs
- บันทึกข้อมูลในฐานข้อมูล
- **เงื่อนไข**: ต้องล็อกอินหลังสมัคร (ไม่ auto-login)

#### 1.2 เข้าสู่ระบบ (Login)
**เทคโนโลยี**: JWT, bcryptjs
- ผู้ใช้กรอกอีเมลและรหัสผ่าน
- ตรวจสอบรหัสผ่านด้วย bcryptjs
- สร้าง JWT token
- เก็บ token ใน localStorage (frontend)
- ส่ง token ไปกับทุก request

#### 1.3 จัดการโปรไฟล์ (Edit Profile)
**เทคโนโลยี**: JWT, PostgreSQL
- แก้ไขชื่อ, คณะ, รูปโปรไฟล์
- อัปเดตข้อมูลในฐานข้อมูล
- Real-time update ผ่าน Socket.io

#### 1.4 รีเซ็ตรหัสผ่าน (Password Reset)
**เทคโนโลยี**: Nodemailer, JWT
- ผู้ใช้กรอกอีเมล
- สร้าง reset token
- ส่งอีเมลพร้อมลิงก์รีเซ็ต
- ตรวจสอบ token และอัปเดตรหัสผ่าน

---

### 2. Item Management

#### 2.1 โพสต์สินค้า (Post Item)
**เทคโนโลยี**: PostgreSQL, Content Moderation, Image Validation
**เงื่อนไข**:
- ต้องล็อกอิน
- ต้องกรอก: ชื่อสินค้า, หมวดหมู่, สภาพสินค้า, รูปภาพ
- เลือกประเภท: Exchange หรือ Donation
- ถ้าเป็น Exchange ต้องกรอก "Looking for"
- ตั้งวันที่หมดอายุ (ห้ามเป็นอดีต)
- ตรวจสอบเนื้อหาไม่เหมาะสม
- ตรวจสอบรูปภาพ (ขนาดไม่เกิน 5MB, รองรับ JPEG/PNG/WebP/GIF)
- ตรวจสอบเนื้อหาซ้ำ (ห้ามโพสต์ชื่อเดียวกันภายใน 24 ชั่วโมง)

**การทำงาน**:
1. ตรวจสอบข้อมูลและเนื้อหา
2. อัปเดตฐานข้อมูล
3. คำนวณ CO₂ footprint
4. Emit Socket.io event (`item:created`)
5. Real-time update หน้า HomePage

#### 2.2 แก้ไขโพสต์ (Edit Item)
**เทคโนโลยี**: PostgreSQL, Content Moderation
**เงื่อนไข**:
- เฉพาะเจ้าของโพสต์เท่านั้น
- ห้ามแก้ไขถ้ามี Exchange/Donation Request ที่ถูกยอมรับแล้ว
- ตรวจสอบเนื้อหาเหมือนตอนโพสต์

**การทำงาน**:
1. ตรวจสอบสิทธิ์
2. ตรวจสอบเนื้อหา
3. อัปเดตฐานข้อมูล
4. Emit Socket.io event (`item:updated`)

#### 2.3 ลบโพสต์ (Delete Item)
**เทคโนโลยี**: PostgreSQL
**เงื่อนไข**:
- เฉพาะเจ้าของโพสต์เท่านั้น
- ห้ามลบถ้ามี Exchange/Donation Request ที่ถูกยอมรับแล้ว

**การทำงาน**:
1. ตรวจสอบสิทธิ์
2. ลบข้อมูลจากฐานข้อมูล (CASCADE)
3. Emit Socket.io event (`item:deleted`)

#### 2.4 ระบบหมดอายุอัตโนมัติ (Auto Expiration)
**เทคโนโลยี**: PostgreSQL, Date Comparison
**เงื่อนไข**:
- ตรวจสอบวันที่หมดอายุ (available_until)
- ถ้า available_until <= CURRENT_DATE และ status != 'exchanged' → is_expired = true
- โพสต์ที่หมดอายุจะไม่แสดงในหน้า Feed
- แสดงในแท็บ "Expired" ในหน้า Profile

**การทำงาน**:
- ใช้ SQL `CASE` statement เพื่อคำนวณ is_expired
- กรองโพสต์ที่หมดอายุออกจาก Feed
- แสดงใน ProfilePage แท็บ "Expired"

---

### 3. Exchange System

#### 3.1 ขอแลกเปลี่ยน (Request Exchange)
**เทคโนโลยี**: PostgreSQL, Socket.io, Email
**เงื่อนไข**:
- ต้องล็อกอิน
- ไม่ใช่เจ้าของโพสต์
- สินค้ายังไม่ถูกแลกเปลี่ยน
- สินค้าเป็นประเภท Exchange

**การทำงาน**:
1. ตรวจสอบเงื่อนไข
2. สร้าง Exchange Request (status: 'pending')
3. สร้าง Chat (status: 'pending')
4. ส่ง Notification ไปยังเจ้าของโพสต์
5. ส่งอีเมลแจ้งเตือน
6. Emit Socket.io event

#### 3.2 ยอมรับ/ปฏิเสธคำขอแลกเปลี่ยน (Accept/Reject)
**เทคโนโลยี**: PostgreSQL, Socket.io, Email
**เงื่อนไข**:
- เจ้าของโพสต์: ยอมรับ/ปฏิเสธคำขอ
- ผู้ขอ: ยอมรับหลังจากเจ้าของยอมรับแล้ว

**การทำงาน**:
1. **Owner Accept**:
   - อัปเดต owner_accepted = true
   - ส่ง Notification ไปยังผู้ขอ
   - ส่งอีเมลแจ้งเตือน

2. **Requester Accept** (หลังจาก Owner Accept):
   - อัปเดต requester_accepted = true
   - เปลี่ยน status เป็น 'chatting'
   - สร้าง QR Code (EX + 8 หลัก)
   - เปลี่ยน item status เป็น 'in_progress'
   - เปิด Chat ให้ส่งข้อความได้

3. **Reject**:
   - เปลี่ยน status เป็น 'rejected'
   - ส่ง Notification
   - ส่งอีเมลแจ้งเตือน

#### 3.3 ยืนยันการแลกเปลี่ยนด้วย QR Code
**เทคโนโลยี**: QR Code Generation, PostgreSQL
**เงื่อนไข**:
- ทั้งสองฝ่ายต้องยอมรับแล้ว
- มี QR Code แล้ว
- ต้องสแกน QR Code ที่ถูกต้อง

**การทำงาน**:
1. สร้าง QR Code: `EX` + 8 หลัก (random)
2. เก็บ QR Code ในฐานข้อมูล
3. เมื่อสแกน QR Code:
   - ตรวจสอบ QR Code
   - สร้าง Exchange History
   - คำนวณ CO₂ ที่ลดได้ (80% ของ CO₂ footprint)
   - เปลี่ยน item status เป็น 'exchanged'
   - เปลี่ยน exchange_request status เป็น 'completed'
   - Emit Socket.io event (`exchange:completed`)

#### 3.4 ประวัติการแลกเปลี่ยน (Exchange History)
**เทคโนโลยี**: PostgreSQL, SQL JOIN
**การทำงาน**:
- ดึงข้อมูล Exchange History
- แสดงข้อมูลสินค้าทั้งสองฝ่าย (ของฉัน + ที่ได้รับ)
- แสดงรูปภาพทั้งสองฝ่าย
- แสดง CO₂ ที่ลดได้

---

### 4. Donation System

#### 4.1 โพสต์สินค้าบริจาค (Post Donation Item)
**เทคโนโลยี**: PostgreSQL, Content Moderation
**เงื่อนไข**:
- เหมือนกับการโพสต์สินค้าปกติ
- เลือก listing_type = 'donation'
- ไม่ต้องกรอก "Looking for"

**การทำงาน**:
- เหมือน Item Management แต่ listing_type = 'donation'

#### 4.2 ขอรับบริจาค (Request Donation)
**เทคโนโลยี**: PostgreSQL, Socket.io, Email
**เงื่อนไข**:
- ต้องล็อกอิน
- ไม่ใช่เจ้าของโพสต์
- สินค้าเป็นประเภท Donation
- ต้องกรอก: ชื่อผู้รับบริจาค, ข้อมูลติดต่อ
- ข้อความเพิ่มเติม (ไม่บังคับ)

**การทำงาน**:
1. ตรวจสอบเงื่อนไข
2. สร้าง Donation Request (status: 'pending')
   - เก็บ recipient_name, recipient_contact
3. สร้าง Chat (status: 'pending')
4. ส่ง Notification ไปยังเจ้าของโพสต์
5. ส่งอีเมลแจ้งเตือน (รวมข้อมูลผู้รับบริจาค)
6. Emit Socket.io event

#### 4.3 ยอมรับ/ปฏิเสธคำขอรับบริจาค (Accept/Reject)
**เทคโนโลยี**: PostgreSQL, Socket.io, Email
**เงื่อนไข**:
- เหมือน Exchange System

**การทำงาน**:
1. **Owner Accept**:
   - เหมือน Exchange
   - แสดงข้อมูลผู้รับบริจาค (recipient_name, recipient_contact)

2. **Requester Accept**:
   - เหมือน Exchange
   - สร้าง QR Code (DN + 8 หลัก)

3. **Reject**:
   - เหมือน Exchange

#### 4.4 ยืนยันการบริจาคด้วย QR Code
**เทคโนโลยี**: QR Code Generation, PostgreSQL
**การทำงาน**:
1. สร้าง QR Code: `DN` + 8 หลัก (random)
2. เมื่อสแกน QR Code:
   - ตรวจสอบ QR Code
   - สร้าง Donation History
   - คำนวณ CO₂ ที่ลดได้ (80% ของ CO₂ footprint)
   - เปลี่ยน item status เป็น 'donated'
   - เปลี่ยน donation_request status เป็น 'completed'
   - Emit Socket.io event (`donation:completed`)

#### 4.5 ประวัติการบริจาค (Donation History)
**เทคโนโลยี**: PostgreSQL, SQL JOIN
**การทำงาน**:
- ดึงข้อมูล Donation History
- แสดงข้อมูลสินค้าที่บริจาค
- แสดงข้อมูลผู้รับบริจาค (ถ้ามี)
- แสดง CO₂ ที่ลดได้

---

### 5. Real-time Chat

#### 5.1 แชทแบบ Real-time
**เทคโนโลยี**: Socket.io (Server + Client)
**เงื่อนไข**:
- ต้องล็อกอิน
- ทั้งสองฝ่ายต้องยอมรับแล้ว (owner_accepted && requester_accepted)
- Chat status = 'active'

**การทำงาน**:
1. **Connection**:
   - Client เชื่อมต่อ Socket.io ด้วย JWT token
   - Server ตรวจสอบ token และ authenticate
   - Join room ตาม chat_id

2. **Send Message**:
   - Client ส่งข้อความผ่าน Socket.io
   - Server ตรวจสอบสิทธิ์
   - บันทึกข้อความในฐานข้อมูล
   - Emit ข้อความไปยัง room (ทั้งสองฝ่าย)

3. **Receive Message**:
   - Client รับข้อความแบบ real-time
   - แสดงข้อความทันที (ไม่ต้อง refresh)

4. **Typing Indicator** (ถ้ามี):
   - แสดงสถานะกำลังพิมพ์

#### 5.2 ยอมรับ/ปฏิเสธแชท (Accept/Decline Chat)
**เทคโนโลยี**: PostgreSQL, Socket.io
**เงื่อนไข**:
- Chat status = 'pending'
- ยังไม่มี QR Code

**การทำงาน**:
1. **Accept**:
   - อัปเดต owner_accepted หรือ requester_accepted = true
   - ถ้าทั้งสองฝ่ายยอมรับ:
     - เปลี่ยน status = 'active'
     - สร้าง QR Code
     - เปลี่ยน item status = 'in_progress'
   - เปิด Chat ให้ส่งข้อความได้

2. **Decline**:
   - เปลี่ยน status = 'rejected'
   - ปิด Chat
   - Revert item status = 'active'

---

### 6. QR Code System

#### 6.1 สร้าง QR Code
**เทคโนโลยี**: qrcode.react, Random Generation
**การทำงาน**:
- **Exchange**: `EX` + 8 หลัก (random)
- **Donation**: `DN` + 8 หลัก (random)
- เก็บ QR Code ในฐานข้อมูล (chats table)
- แสดง QR Code ใน Chat Modal

#### 6.2 สแกน QR Code
**เทคโนโลยี**: html5-qrcode
**การทำงาน**:
- ใช้กล้องสแกน QR Code
- ตรวจสอบรูปแบบ (EX หรือ DN)
- ตรวจสอบ QR Code ในฐานข้อมูล

#### 6.3 ยืนยัน QR Code
**เทคโนโลยี**: PostgreSQL
**เงื่อนไข**:
- QR Code ต้องถูกต้อง
- ยังไม่เคยยืนยัน

**การทำงาน**:
1. ตรวจสอบ QR Code
2. อัปเดต qr_confirmed = true
3. สร้าง Exchange/Donation History
4. คำนวณ CO₂ ที่ลดได้
5. เปลี่ยน item status
6. Emit Socket.io event

---

### 7. Notification System

#### 7.1 แจ้งเตือนแบบ Real-time
**เทคโนโลยี**: Socket.io, PostgreSQL
**ประเภทการแจ้งเตือน**:
- `exchange_request`: มีคำขอแลกเปลี่ยนใหม่
- `exchange_accepted`: คำขอแลกเปลี่ยนถูกยอมรับ
- `exchange_rejected`: คำขอแลกเปลี่ยนถูกปฏิเสธ
- `donation_request`: มีคำขอรับบริจาคใหม่
- `donation_accepted`: คำขอรับบริจาคถูกยอมรับ
- `donation_rejected`: คำขอรับบริจาคถูกปฏิเสธ
- `chat_message`: มีข้อความใหม่
- `exchange_completed`: การแลกเปลี่ยนสำเร็จ
- `donation_completed`: การบริจาคสำเร็จ

**การทำงาน**:
1. สร้าง Notification ในฐานข้อมูล
2. Emit Socket.io event (`notification:new`)
3. Client รับ notification แบบ real-time
4. แสดง notification badge
5. ทำเครื่องหมายว่าอ่านแล้ว

---

### 8. CO₂ Tracking

#### 8.1 คำนวณ CO₂ Footprint
**เทคโนโลยี**: Custom Calculator (co2Calculator.js)
**การทำงาน**:
- คำนวณตามหมวดหมู่สินค้า:
  - Electronics: 50-200 kg CO₂e
  - Clothes: 20-50 kg CO₂e
  - Books: 5-10 kg CO₂e
  - Furniture: 100-300 kg CO₂e
  - etc.
- คำนวณตามสภาพสินค้า:
  - New: 100%
  - Like New: 80%
  - Good: 60%
  - Fair: 40%
  - Poor: 20%

#### 8.2 คำนวณ CO₂ ที่ลดได้
**การทำงาน**:
- **Exchange**: 80% ของ CO₂ footprint
- **Donation**: 80% ของ CO₂ footprint
- บันทึกใน Exchange/Donation History

#### 8.3 สถิติ CO₂
**การทำงาน**:
- รวม CO₂ ที่ลดได้ทั้งหมด
- แสดงในหน้า Statistics

---

### 9. Content Moderation

#### 9.1 ตรวจสอบคำต้องห้าม (Spam Detection)
**เทคโนโลยี**: Custom Moderation (contentModeration.js)
**การตรวจสอบ**:
- คำต้องห้าม (profanity, inappropriate words)
- การซ้ำตัวอักษรมากเกินไป
- อักขระพิเศษมากเกินไป
- ตัวพิมพ์ใหญ่เกินไป

**การทำงาน**:
- ใช้ word boundary regex
- ตรวจสอบทั้ง Title, Description, Looking For

#### 9.2 ตรวจสอบเนื้อหาซ้ำ (Duplicate Content Check)
**เทคโนโลยี**: PostgreSQL
**การทำงาน**:
- ตรวจสอบชื่อสินค้าซ้ำ (ภายใน 24 ชั่วโมง)
- ตรวจสอบ description ซ้ำ (ภายใน 1 ชั่วโมง)

#### 9.3 ตรวจสอบรูปภาพ (Image Validation)
**เทคโนโลยี**: Base64/URL Validation
**การทำงาน**:
- ตรวจสอบรูปแบบ (JPEG, PNG, WebP, GIF)
- ตรวจสอบขนาด (ไม่เกิน 5MB)
- ตรวจสอบ URL format

---

### 10. Email Service

#### 10.1 ส่งอีเมลแจ้งเตือน
**เทคโนโลยี**: Nodemailer
**รองรับ**:
- Gmail (OAuth2)
- SMTP

**ประเภทอีเมล**:
- Exchange Request Notification
- Exchange Accepted/Rejected
- Donation Request Notification
- Donation Accepted/Rejected
- Password Reset

**การทำงาน**:
1. ตั้งค่า Nodemailer
2. สร้าง HTML email template
3. ส่งอีเมล
4. Handle errors

---

### 11. Statistics

#### 11.1 สถิติการแลกเปลี่ยน/บริจาค
**เทคโนโลยี**: PostgreSQL, SQL Aggregation
**ข้อมูล**:
- จำนวนการแลกเปลี่ยนทั้งหมด
- จำนวนการบริจาคทั้งหมด
- CO₂ ที่ลดได้ทั้งหมด
- จำนวนผู้ใช้
- จำนวนสินค้า

**การทำงาน**:
- Query ข้อมูลจากฐานข้อมูล
- คำนวณสถิติ
- แสดงในหน้า Statistics

---

## 🗄️ ฐานข้อมูล

### Tables

1. **users**: ข้อมูลผู้ใช้
   - id, name, faculty, email, password_hash, avatar_url, created_at

2. **items**: ข้อมูลสินค้า
   - id, user_id, title, category, item_condition, looking_for, description, available_until, image_url, status, listing_type, created_at, updated_at

3. **exchange_requests**: คำขอแลกเปลี่ยน
   - id, item_id, requester_id, message, status, owner_accepted, requester_accepted, requester_item_name, requester_item_image_url, requester_item_category, created_at, updated_at

4. **exchange_history**: ประวัติการแลกเปลี่ยน
   - id, exchange_request_id, item_id, owner_id, requester_id, co2_reduced, exchanged_at, created_at

5. **donation_requests**: คำขอรับบริจาค
   - id, item_id, requester_id, recipient_name, recipient_contact, message, status, owner_accepted, requester_accepted, created_at, updated_at

6. **donation_history**: ประวัติการบริจาค
   - id, item_id, donor_id, recipient_id, recipient_name, recipient_contact, donation_location, message, status, co2_reduced, donated_at, created_at

7. **chats**: ข้อมูลแชท
   - id, item_id, exchange_request_id, donation_request_id, creator_id, participant_id, status, owner_accepted, requester_accepted, qr_code, qr_confirmed, qr_confirmed_at, closed_at, created_at, updated_at

8. **messages**: ข้อความในแชท
   - id, chat_id, sender_id, body, created_at

9. **notifications**: การแจ้งเตือน
   - id, user_id, title, body, type, metadata, read, created_at

---

## 🔐 Security Features

1. **Authentication**: JWT tokens
2. **Password Hashing**: bcryptjs
3. **Input Validation**: express-validator, Zod
4. **Content Moderation**: Spam detection, duplicate check
5. **CORS**: Configured allowed origins
6. **SQL Injection Prevention**: Parameterized queries
7. **Rate Limiting**: (สามารถเพิ่มได้)

---

## 📱 Real-time Features

1. **Socket.io Events**:
   - `item:created`: สร้างสินค้าใหม่
   - `item:updated`: อัปเดตสินค้า
   - `item:deleted`: ลบสินค้า
   - `exchange:completed`: การแลกเปลี่ยนสำเร็จ
   - `donation:completed`: การบริจาคสำเร็จ
   - `notification:new`: การแจ้งเตือนใหม่
   - `message:new`: ข้อความใหม่

---

## 🚀 Deployment

### Backend
- Port: 4000 (default)
- Environment Variables: `.env`

### Frontend
- Port: 3000 (default)
- Build: `npm run build`

---

## 📝 Notes

- ระบบรองรับทั้งภาษาไทยและภาษาอังกฤษ
- Real-time updates ทำงานผ่าน Socket.io
- QR Code system รองรับทั้ง Exchange และ Donation
- Content moderation ป้องกัน spam และเนื้อหาไม่เหมาะสม
- CO₂ tracking ช่วยติดตามผลกระทบต่อสิ่งแวดล้อม

