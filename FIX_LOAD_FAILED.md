# 🔧 แก้ไขปัญหา: "Load failed" เมื่อส่งคำขอแลกเปลี่ยน

## ❌ ปัญหา

เมื่อกดปุ่ม "Send Exchange Request" จะเกิด error "Load failed"

## 🔍 สาเหตุ

**Backend server ไม่ได้รันอยู่!**

Frontend พยายามเรียก API `POST /api/exchange` แต่ backend ไม่ได้รัน เลยได้ error "Load failed"

---

## ✅ วิธีแก้ไข

### 1. รัน Backend Server

เปิด Terminal ใหม่และรัน:

```bash
cd /Users/pmykingg/Documents/project/hackkathon2025byg4/backend
npm run dev
```

หรือจาก root directory:

```bash
cd /Users/pmykingg/Documents/project/hackkathon2025byg4
npm run start:backend
```

### 2. ตรวจสอบว่า Backend รันอยู่

คุณควรเห็น:

```
🔍 กำลังตรวจสอบการเชื่อมต่อ Database...
✅ Database connected successfully!
📧 กำลังตรวจสอบ Email Service...
📧 Email Service: MOCK MODE (ไม่ส่งอีเมลจริง แค่ log ใน console)
Backend listening on port 4000
🎉 Server is fully operational.
```

### 3. ทดสอบ API

เปิด browser ไปที่:
```
http://localhost:4000/health
```

ควรเห็น:
```json
{"ok":true}
```

### 4. ลองส่งคำขอแลกเปลี่ยนอีกครั้ง

1. Refresh หน้าเว็บ (Cmd+R หรือ F5)
2. Login (ถ้ายังไม่ได้ login)
3. กด "Exchange Item"
4. กรอกข้อมูลและกด "Send Exchange Request"

---

## 🔍 ตรวจสอบปัญหาเพิ่มเติม

### ถ้ายังมีปัญหา "Load failed"

1. **ตรวจสอบว่า Backend รันอยู่:**
   ```bash
   lsof -ti:4000
   ```
   ถ้าไม่มี output = backend ไม่ได้รัน

2. **ตรวจสอบ Console ใน Browser:**
   - เปิด Developer Tools (Cmd+Option+I)
   - ไปที่ Console tab
   - ดู error message

3. **ตรวจสอบ Network Tab:**
   - ไปที่ Network tab
   - ลองส่งคำขอแลกเปลี่ยนอีกครั้ง
   - ดูว่า request ไปที่ URL ไหน
   - ดู status code (ควรเป็น 200 หรือ 201)

4. **ตรวจสอบ CORS:**
   - ดู error ใน Console
   - ถ้ามี CORS error ตรวจสอบ `CLIENT_ORIGIN` ใน `.env`

---

## 📝 Checklist

- [ ] Backend server รันอยู่ที่ port 4000
- [ ] ตรวจสอบ `/health` endpoint สำเร็จ
- [ ] Frontend เรียก API ไปที่ `http://localhost:4000/api`
- [ ] Login แล้ว (มี token)
- [ ] ไม่มี error ใน browser console
- [ ] Network request สำเร็จ (status 200/201)

---

## 🎯 สรุป

**ปัญหา:** Backend server ไม่ได้รัน  
**แก้ไข:** รัน `npm run dev` ใน backend directory  
**ผลลัพธ์:** API จะทำงานและสามารถส่งคำขอแลกเปลี่ยนได้

---

## 💡 Tips

- เก็บ Terminal ที่รัน backend ไว้เปิดตลอด
- ถ้า backend หยุดทำงาน ให้รันใหม่
- ตรวจสอบ log ใน terminal เพื่อดู error (ถ้ามี)

