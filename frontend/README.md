# CMU ShareCycle - Frontend

เว็บแอปพลิเคชันสำหรับแลกเปลี่ยนสินค้าระหว่างนักศึกษามหาวิทยาลัยเชียงใหม่ (CMU ShareCycle) - ระบบแลกเปลี่ยนสินค้าแบบยั่งยืนสำหรับชุมชน CMU

## 📖 เกี่ยวกับโปรเจ็กต์

CMU ShareCycle เป็นแพลตฟอร์มแลกเปลี่ยนสินค้าสำหรับนักศึกษามหาวิทยาลัยเชียงใหม่ โดยไม่ต้องใช้เงินตรา เพียงแค่แลกเปลี่ยนสินค้าที่คุณมีกับสินค้าที่คุณต้องการ ช่วยลดขยะและสร้างชุมชนที่ยั่งยืน

### ฟีเจอร์หลัก

- 🔐 **ระบบ Authentication** - ลงทะเบียนและเข้าสู่ระบบด้วยอีเมล @cmu.ac.th
- 🏠 **หน้า Home** - ดูรายการสินค้าทั้งหมด ค้นหา และกรองตามหมวดหมู่/สภาพ
- 📝 **โพสต์สินค้า** - โพสต์สินค้าที่ต้องการแลกเปลี่ยน พร้อมรายละเอียดและรูปภาพ
- 💬 **ขอแลกเปลี่ยน** - ส่งคำขอแลกเปลี่ยนสินค้ากับผู้โพสต์
- 👤 **หน้า Profile** - ดูโปรไฟล์ สถิติการแลกเปลี่ยน และประวัติการโพสต์
- 🔔 **การแจ้งเตือน** - รับการแจ้งเตือนเมื่อมีคำขอแลกเปลี่ยนหรือข้อความใหม่
- 💬 **ระบบแชท** - สนทนากับผู้ใช้อื่นเพื่อตกลงรายละเอียดการแลกเปลี่ยน

## 🚀 การติดตั้ง

### 1. ติดตั้ง Dependencies

```bash
npm install
```

### 2. สร้างไฟล์ Environment Variables

สร้างไฟล์ `.env` ในโฟลเดอร์ `frontend/`:

```env
REACT_APP_API_URL=http://localhost:4000/api
```

## 🎯 การรันแอปพลิเคชัน

### Development Mode

```bash
npm start
# หรือ
npm run dev
```

แอปพลิเคชันจะรันที่ `http://localhost:3000`

### Build for Production

```bash
npm run build
```

ไฟล์ที่ build จะอยู่ในโฟลเดอร์ `build/`

## 📁 โครงสร้างโปรเจ็กต์

```
src/
├── components/          # React components
│   ├── layout/         # Header, Footer
│   ├── modals/         # Modal components
│   │   ├── PostItemModal.jsx
│   │   ├── ExchangeRequestModal.jsx
│   │   ├── NotificationsModal.jsx
│   │   └── ChatModal.jsx
│   └── ui/             # UI components
│       └── Modal.jsx
├── pages/              # Page components
│   ├── LoginPage.jsx
│   ├── RegisterPage.jsx
│   ├── HomePage.jsx
│   └── ProfilePage.jsx
├── context/            # React Context
│   └── AuthContext.jsx
├── lib/                # API utilities
│   └── api.js
├── index.css           # Global styles
├── App.jsx             # Main app component
└── index.jsx           # Entry point
```

## 🛠 เทคโนโลยีที่ใช้

- **React 18** - UI Library
- **JavaScript (ES Modules)** - Programming Language
- **Create React App** - Build Tool
- **React Router DOM** - Routing
- **Tailwind CSS** - Styling
- **Lucide React** - Icons
- **Socket.IO Client** - Real-time communication

## 📱 หน้าจอหลัก

### 1. หน้า Login
- เข้าสู่ระบบด้วยอีเมล @cmu.ac.th และรหัสผ่าน
- มีลิงก์ไปยังหน้าสมัครสมาชิก

### 2. หน้า Register
- สมัครสมาชิกด้วยอีเมล @cmu.ac.th
- ระบุชื่อ คณะ และรหัสผ่าน

### 3. หน้า Home
- แสดงรายการสินค้าทั้งหมด
- ค้นหาสินค้า
- กรองตามหมวดหมู่และสภาพสินค้า
- โพสต์สินค้าใหม่

### 4. หน้า Profile
- ดูโปรไฟล์ผู้ใช้
- สถิติการแลกเปลี่ยน (Items Shared, CO₂ Reduced)
- ดูโพสต์ของตัวเอง
- ดูประวัติการแลกเปลี่ยน

## 🔧 การใช้งาน

1. **เริ่มต้นใช้งาน**
   ```bash
   npm start
   ```

2. **เข้าสู่ระบบ**
   - ไปที่หน้า Login
   - กรอกอีเมล @cmu.ac.th และรหัสผ่าน
   - หรือคลิก "สมัครสมาชิก" เพื่อสร้างบัญชีใหม่

3. **โพสต์สินค้า**
   - คลิกปุ่ม "Post Item"
   - กรอกข้อมูลสินค้า (ชื่อ, หมวดหมู่, สภาพ, รูปภาพ)
   - ระบุสิ่งที่ต้องการแลกเปลี่ยน
   - ตั้งวันหมดอายุของโพสต์

4. **ขอแลกเปลี่ยน**
   - คลิกที่สินค้าที่ต้องการ
   - คลิกปุ่ม "Exchange Item"
   - กรอกข้อมูลสินค้าที่จะแลก
   - ส่งข้อความถึงผู้โพสต์ (optional)

5. **แชทกับผู้ใช้**
   - คลิกปุ่ม Message (ล่างขวา)
   - เริ่มแชทด้วยอีเมล @cmu.ac.th
   - สนทนาเพื่อตกลงรายละเอียดการแลกเปลี่ยน

## 🔐 Environment Variables

สร้างไฟล์ `.env` ในโฟลเดอร์ root:

```env
REACT_APP_API_URL=http://localhost:4000/api
```

## 📝 หมายเหตุ

- ต้องใช้อีเมล @cmu.ac.th เท่านั้นในการลงทะเบียน
- รูปภาพจะถูกเก็บเป็น base64 string
- ระบบใช้ Socket.IO สำหรับการแจ้งเตือนและแชทแบบ real-time
- ข้อมูลการแลกเปลี่ยนจะถูกเก็บในฐานข้อมูล PostgreSQL

## 🤝 การมีส่วนร่วม

โปรเจ็กต์นี้เป็นส่วนหนึ่งของโครงการ Hackathon 2025 โดยทีม G4

## 📄 License

Private - สำหรับใช้งานภายในมหาวิทยาลัยเชียงใหม่เท่านั้น
